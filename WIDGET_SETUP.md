# Wasurenai - ウィジェット追加手順

## ウィジェットターゲットの追加

ウィジェット機能を有効にするには、Xcodeで以下の手順を実行してください：

### 1. ウィジェットターゲットを追加

1. Xcodeでプロジェクトを開く
2. **File > New > Target...** を選択
3. **Widget Extension** を選択して **Next**
4. 以下の設定を入力：
   - **Product Name**: `WasurenaiWidget`
   - **Team**: あなたの開発者チーム
   - **Include Configuration Intent**: チェックを外す（Static Configuration使用）
   - **Include Live Activity**: チェックを外す
5. **Finish** をクリック

### 2. 既存のウィジェットファイルを置き換え

Xcodeが自動生成したファイルを以下のファイルで置き換えてください：

- `WasurenaiWidget/WasurenaiWidgetBundle.swift` - 既に作成済み
- `WasurenaiWidget/WasurenaiWidget.swift` - 既に作成済み

### 3. App Group設定（オプション：データ共有用）

メインアプリとウィジェット間でCoreDataを共有する場合：

1. プロジェクト設定で **Signing & Capabilities** を開く
2. **+ Capability** をクリックして **App Groups** を追加
3. `group.com.yourname.wasurenai` のようなグループを作成
4. 同じApp Groupをウィジェットターゲットにも追加

### 4. Persistence.swiftの更新（App Group使用時）

```swift
// shared containerを使用
let container = NSPersistentContainer(name: "Wasurenai")
if let description = container.persistentStoreDescriptions.first {
    let storeURL = FileManager.default.containerURL(
        forSecurityApplicationGroupIdentifier: "group.com.yourname.wasurenai"
    )?.appendingPathComponent("Wasurenai.sqlite")
    description.url = storeURL
}
```

---

## 実装済み機能

### 📱 ウィジェットサイズ

| サイズ | 表示内容 |
|--------|----------|
| **Small** | 最も期限が近いアイテム1件 |
| **Medium** | 期限間近のアイテム3件をカード形式 |
| **Large** | 期限間近のアイテム5件をリスト形式 |

### 🔄 更新頻度

- 1時間ごとに自動更新
- ユーザーがタップするとメインアプリを起動

### 🎨 表示情報

- アイテム名
- アイコン
- カテゴリ（Medium/Largeサイズ）
- 期限ステータス（期限切れ/今日/明日/もうすぐ）
- 日付

---

## トラブルシューティング

### ウィジェットにデータが表示されない

1. App Groupが正しく設定されているか確認
2. 両方のターゲットで同じApp Groupを使用しているか確認
3. `Persistence.swift`でshared containerを使用しているか確認

### ウィジェットが更新されない

1. ホーム画面でウィジェットを長押し → **ウィジェットを編集** で更新
2. Xcodeでウィジェットターゲットを選択してビルド・実行
